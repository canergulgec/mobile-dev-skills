---
name: gradle-build
description: Build the Android project using Gradle
allowed-tools: Bash(./gradlew *)
---

Build the Android project using Gradle.

Steps:
1. Run: `./gradlew assembleDebug`
2. Report any errors with file path and line number clearly
3. If the build succeeds, show the output APK path
